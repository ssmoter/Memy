@page "/counter"
@using Memy.Shared;
@inject HttpClient Http

<PageTitle>Counter</PageTitle>

<h1>Counter</h1>

<p role="status">Current count: @currentCount</p>

<button class="btn btn-primary" @onclick="IncrementCount">Click me</button>

@code {
    private int currentCount = 0;

    private async void IncrementCount()
    {
        var content = new Memy.Shared.Model.UserSimple()
            {
                Email = "slaweksmoter82@gmail.com",
                Password = "slaweksmoter82@gmail.com"
            };

        var forecasts = await Http.GetFromJsonAsync<WeatherForecast[]>("WeatherForecast");
        var result = await Http.PostAsJsonAsync("api/UserLog", content);

        currentCount++;
    }
}
